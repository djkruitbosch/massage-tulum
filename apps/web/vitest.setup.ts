/**
 * Vitest global test setup.
 *
 * Extends Vitest's expect with @testing-library/jest-dom matchers
 * (toBeInTheDocument, toHaveAttribute, etc.) for component tests.
 */
import '@testing-library/jest-dom';
